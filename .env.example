PORT="" 				# Provide a value for PORT
MONGO_URI="" 				# Provide a value for MONGO_URI
JWT_SECRET="" 				# Provide a value for JWT_SECRET
JWT_EXPIRES_IN="" 				# Provide a value for JWT_EXPIRES_IN
CORS_ORIGIN="" 				# Provide a value for CORS_ORIGIN
AUTH_RATE_WINDOW_MS="" 			# Login rate-limit window in milliseconds (default: 600000)
AUTH_RATE_LIMIT="" 				# Max login requests per window per IP (default: 10)
AUTH_LOCK_MAX_ATTEMPTS="" 		# Failed login attempts before lockout (default: 5)
AUTH_LOCK_WINDOW_MS="" 			# Account lock duration in milliseconds (default: 900000)
